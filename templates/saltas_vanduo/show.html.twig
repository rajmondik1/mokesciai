{% extends 'base.html.twig' %}

{% block title %}SaltasVanduo{% endblock %}

{% block body %}
    <h1>SaltasVanduo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saltas_vanduo.id }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ saltas_vanduo.userid }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ saltas_vanduo.date ? saltas_vanduo.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Iki</th>
                <td>{{ saltas_vanduo.iki }}</td>
            </tr>
            <tr>
                <th>Nuo</th>
                <td>{{ saltas_vanduo.nuo }}</td>
            </tr>
            <tr>
                <th>Tarifas</th>
                <td>{{ saltas_vanduo.tarifas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('saltas_vanduo_index') }}">back to list</a>

    <a href="{{ path('saltas_vanduo_edit', {'id': saltas_vanduo.id}) }}">edit</a>

    {{ include('saltas_vanduo/_delete_form.html.twig') }}
{% endblock %}