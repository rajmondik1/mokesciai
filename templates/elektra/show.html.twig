{% extends 'base.html.twig' %}

{% block title %}Elektra{% endblock %}

{% block body %}
    <h1>Elektra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elektra.id }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ elektra.userid }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ elektra.date ? elektra.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Iki</th>
                <td>{{ elektra.iki }}</td>
            </tr>
            <tr>
                <th>Nuo</th>
                <td>{{ elektra.nuo }}</td>
            </tr>
            <tr>
                <th>Tarifas</th>
                <td>{{ elektra.tarifas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('elektra_index') }}">back to list</a>

    <a href="{{ path('elektra_edit', {'id': elektra.id}) }}">edit</a>

    {{ include('elektra/_delete_form.html.twig') }}
{% endblock %}